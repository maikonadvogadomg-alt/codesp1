Assunto: Registro de chave pública e informações SSO para integração

Prezados,

Solicitamos o registro da chave pública anexa para integração com o Portal de Serviços da PDPJ.

- Nome da instituição / responsável: [Seu nome ou organização]
- Contato técnico: [nome, e‑mail, telefone]
- Finalidade: Autenticação via JWT assinada (RS256) usada como `client_assertion` no fluxo OAuth2 (client_credentials / jwt-bearer) para chamadas à API do Portal de Serviços.
- Arquivo anexo: maikon.pub.pem (chave pública RSA)

Requisições:
- Por favor registrem a chave pública e associem‑na ao `client_id` que será utilizado pela nossa integração (se já houver um `client_id`, favor confirmar qual é).
- Informar o `token_endpoint` (URL) do SSO da PDPJ.
- Informar os escopos recomendados para uso com a API (ex.: `api`, `openid api`).
- Se houver requisitos específicos de claims no `client_assertion` (ex.: `iss`, `aud`, `sub`), por favor indicar o formato esperado.

Observações de segurança:
A chave privada correspondente não será compartilhada — usaremos `client_assertion` assinada localmente com RS256. Solicitamos apenas o registro público para validação das assinaturas no servidor.

Aguardo confirmação do registro e as informações solicitadas para que possamos realizar testes e trocar o JWT por `access_token` junto ao token endpoint.

Atenciosamente,
[Seu nome]
[Organização]
[Contato técnico: e‑mail / telefone]


Observação técnica (opcional para o time PDPJ):
- O arquivo `maikon.pub.pem` foi gerado a partir da chave privada e está disponível para envio. Ele contém a chave pública no formato PEM.
- Caso prefiram, podemos fornecer também o `jti`/`iss`/`aud`/`sub` que utilizaremos no `client_assertion` durante os testes.

Chave pública (`maikon.pub.pem`):
-----BEGIN PUBLIC KEY-----

rwIDAQAB
-----END PUBLIC KEY-----

